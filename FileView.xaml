<UserControl x:Class="Pin.FileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Pin"
             mc:Ignorable="d" 
             d:DesignHeight="23" d:DesignWidth="300">
    <UserControl.DataContext>
        <local:FileView/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <BitmapImage x:Key="DefaultImage" UriSource="/images/Exit.png"/> 
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="23"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Margin="4">
            <Image Source="{Binding ImageSource,FallbackValue={StaticResource DefaultImage}}"/>
        </Grid>
        <TextBlock VerticalAlignment="Center" Grid.Column="1" Text="{Binding TextSource,FallbackValue='Loading...'}"/>
    </Grid>
</UserControl>
